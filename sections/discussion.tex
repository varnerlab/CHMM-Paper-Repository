\paragraph{Replicating the slow-ACF behaviour at $K \ge 3$.}
Two issues compound in the \citet{ryden1998stylized} low-$K$ setup: a single self-transition rate produces a single exponential mode that cannot be simultaneously fast and slow, and the random initialisations standard in the 1990s often converge to degenerate local optima. Our $K = 2$ replication on SPY (Appendix~\ref{sec:ryden_replication}) clarifies the mechanism: ACF-MAE is essentially constant at $\approx 0.050$ across quantile and five random-seed initialisations. The binding low-$K$ constraint is therefore distributional (KS at $K = 2$ drops to $67$--$72\%$): with modern initialisation the ACF is reproduced at any $K \ge 2$, while distributional fidelity drives the choice of $K$. The effective-rank diagnostic in Appendix~\ref{sec:spectral_rank} makes this microscopically explicit: at the fitted $\hat{\mathbf T}$ for $K^\star = 3$ the dominant non-unit mode alone carries $96.8\%$ of the lag-1 absolute-return ACF on SPY.

\paragraph{Closing the kurtosis gap with heavy-tailed emissions.}
CHMM-N at $K^\star = 3$ produces simulated IS kurtosis $3.83$ against observed $7.68$: a three-component Gaussian mixture's between-component term is bounded and cannot tile the empirical heavy tail. CHMM-t addresses this through per-state Student-$t$ emissions, with the shared-$\nu$ ablation as the headline heavy-tail recipe (simulated IS kurtosis $4.68$ at $K^\star = 3$, no penalty hyperparameter; Appendix~\ref{sec:chmm_t_shared_nu}). The penalised per-state CHMM-t at $\lambda = 20$ overshoots aggregate IS kurtosis to $14.91$ at $K^\star = 3$ because the lower-bracket-pinned state carries a larger share of the three-state mixture; $\lambda = 20$ was tuned at $K = 18$ (Appendix~\ref{sec:lambda_cv_pre2020}) and is reported at $K^\star = 3$ as an upper bound on the unpenalised heavy tail. The natural alternative of raising $\nu_{\min}$ above $2.1$ to remove the bracket-pinning state directly is reported as a sensitivity panel in Appendix~\ref{sec:nu_diagnostics}. CHMM-L sits between CHMM-N and CHMM-t at IS kurtosis $5.30$ without any shape parameter; CHMM-GED's bimodal $\hat p_k$ partition (Appendix~\ref{sec:supp_p_partition}) is the data-driven analog of a hand-classified Gaussian-bulk / Laplace-tail hybrid. The four-way variant decision (use-case to recommended family) is summarised in the variant decision guide of Appendix~\ref{sec:variant_decision_guide}.

\paragraph{Stationarity scope and operational deployment.}
The CHMM is trained on the assumption that returns are locally stationary across the IS window. Single-name equity returns are well known to carry sector-specific structural breaks (\citealp{pastorstambaugh2001equity}; \citealp{andreoughysels2002breaks}; surveyed by \citealp{angtimmermann2012regime}); the cross-ticker generalisation in \S\ref{sec:cross_asset_univariate} reflects this directly, with $11/30$ tickers below $60\%$ OoS KS, concentrated in single-name regime introductions. The mitigation is periodic refit (\citealp{pesarantimmermann2007window}; an online-EM formulation by \citealp{cappe2011online}): a quarterly-refit version of the cross-ticker panel (Appendix~\ref{sec:cross_ticker_quarterly_refit}) reduces failures from $11/30$ to $7/30$ at the $K = 18$ kurtosis-fidelity reference. The walk-forward W2 (COVID) and W4 (2022 rate-hike onset) stress folds remain out-of-distribution by KS for every generator under any refit cadence (Appendix~\ref{sec:supp_misc}); these are regime introductions the IS distribution does not span.

\paragraph{Stylized-fact scope and parameter parsimony.}
The Cont (2001) stylized-fact list also includes the leverage effect (negative $\mathrm{Corr}(G_t, |G_{t+1}|)$ on equity returns); the present paper's body evaluation covers only the three symmetric stylized facts and treats the leverage-axis behaviour of symmetric-emission CHMMs as a deferred topic (Appendix~\ref{sec:leverage_effect}). On parametric complexity, CHMM-N at $K = 18$ has $K(K-1) + 2K = 342$ free parameters (CHMM-t adds $K$ degrees-of-freedom, CHMM-GED adds $K$ shape parameters); the QuantGAN baseline of \citet{wiese2020quantgan} carries on the order of $10^4$ to $10^5$, three orders of magnitude higher.

