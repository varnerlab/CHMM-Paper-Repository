\paragraph{Reminder on Pipeline~A vs Pipeline~B.}
Subsections \ref{sec:descriptive}--\ref{sec:cross_asset_univariate} and \ref{sec:var_backtest} operate under \textbf{Pipeline~A} (single-asset CHMM fit per ticker, no cross-asset coupling); only \S\ref{sec:cross_asset} operates under \textbf{Pipeline~B} (Pipeline~A marginals plus a rank-based copula, Figure~\ref{fig:pipeline_schematic}).

\subsection{Descriptive Statistics and Stylized Facts}
\label{sec:descriptive}

All three canonical stylized facts are present in both the IS ($T = 2{,}516$) and OoS ($T = 572$) windows.
The IS excess growth rates exhibit excess kurtosis of $7.678$, strong negative skewness ($-0.751$), and a Jarque--Bera statistic of $6{,}416.6$, decisively rejecting normality ($p < 0.001$).
The Ljung--Box test on absolute returns rejects no autocorrelation ($\text{LB} = 2{,}959.3$, $p < 0.001$), confirming pronounced volatility clustering.
The Ljung--Box test on raw returns is small at the same lag ($\text{LB}_{G} \approx 5.5$ at lag $20$, well below the $\chi^2_{20}$ critical value of $31.4$), confirming negligible linear autocorrelation.
The OoS window shares the same qualitative profile (excess kurtosis $5.294$, $\text{LB}_{|G|} = 250.6$).
Figure~\ref{fig:stylized_facts} summarises the three stylized facts visually on the IS window: a heavy-tailed marginal that fits the Laplace shape better than the Gaussian (panel a), the corresponding Q-Q deviation from normality (panel b), a raw-return ACF that sits inside the $99\%$ Bartlett band at every lag beyond a small lag-1 microstructure spike (panel c), and a slowly decaying absolute-return ACF that remains outside the band over many lags (panel d).

\begin{figure}[t]
\centering
\begin{subfigure}[b]{0.49\textwidth}\centering
\includegraphics[width=\textwidth]{figs/Fig-1-Stylized-Facts-a.pdf}
\caption{Marginal density.}
\end{subfigure}\hfill
\begin{subfigure}[b]{0.49\textwidth}\centering
\includegraphics[width=\textwidth]{figs/Fig-1-Stylized-Facts-b.pdf}
\caption{Normal Q-Q plot.}
\end{subfigure}\\[0.5em]
\begin{subfigure}[b]{0.49\textwidth}\centering
\includegraphics[width=\textwidth]{figs/Fig-1-Stylized-Facts-c.pdf}
\caption{ACF of raw $G_t$.}
\end{subfigure}\hfill
\begin{subfigure}[b]{0.49\textwidth}\centering
\includegraphics[width=\textwidth]{figs/Fig-1-Stylized-Facts-d.pdf}
\caption{ACF of $|G_t|$.}
\end{subfigure}
\caption{\textbf{The three Cont stylized facts on SPY IS}. Panel~(a): empirical IS marginal density of the annualised excess log return $G_t$ with maximum-likelihood Gaussian (blue dashed) and Laplace (red) overlays; Laplace tracks the central peak and tails decisively better. Panel~(b): normal Q-Q plot showing the heavy-tailed deviation from the $45^\circ$ reference. Panel~(c): empirical ACF of raw $G_t$ at lags $1$--$252$, with the $99\%$ Bartlett confidence band (dashed grey); only lag~$1$ exits the band (microstructure noise), lags $2$--$252$ sit inside, confirming negligible linear autocorrelation. Panel~(d): empirical ACF of $|G_t|$ on the same window, with the same $99\%$ band; the slow decay over many lags is the volatility-clustering signature that motivates the spectral identity~\eqref{eq:acf_normalised}.}
\label{fig:stylized_facts}
\end{figure}

\subsection{State-Count Selection: Two Reference Operating Points}
\label{sec:k_selection_results}

We sweep $K \in \{3, 6, 9, 12, 15, 18, 21\}$ for CHMM-N on the SPY IS window and score each fit on (i) held-out log-likelihood on a clean pre-OoS validation slice (estimation 2014-01-03 to 2021-12-31, validation 2022-01-03 to 2024-01-03), (ii) the four information criteria of \citet{nguyen2018hidden} (Akaike information criterion (AIC), Bayesian information criterion (BIC), Hannan-Quinn criterion (HQC), consistent AIC (CAIC), $p = K^2 + 2K - 1$), and (iii) the IS and OoS two-sample Kolmogorov--Smirnov pass rates over $1{,}000$ simulated paths.

The held-out criterion picks $K^\star = 3$: validation log-likelihood per observation degrades monotonically from $-2.5345$ at $K = 3$ to $-2.5847$ at $K = 21$, a difference of $25.2$ nats over the $T_{\text{val}} = 502$-day validation slice. BIC on the estimation window agrees ($K^\star = 3$). Distributional fidelity, however, rises across the sweep before plateauing: IS KS rises from $89.7\%$ at $K = 3$ to $94.7\%$ at $K = 18$. The absolute-return ACF-MAE is remarkably stable across the entire sweep ($0.047$ to $0.053$), and the raw-return ACF-MAE is essentially flat at $\approx 0.024$ across the same sweep, consistent with the spectral identity~\eqref{eq:acf_normalised}: the absolute-return ACF is determined by the eigenvalue spectrum of $\mathbf T$, which the quantile-initialized Baum-Welch recovers at every $K \ge 3$, and the raw-return ACF is null in expectation under any HMM with i.i.d.\ within-state emissions.

We therefore report two reference operating points throughout this section. $K^\star = 3$ is the held-out-likelihood and BIC choice, the methodologically conservative default. $K = 18$ is the explicit tail-fidelity operating point, chosen on a multi-objective criterion combining IS KS plateau, IS / OoS distributional pass rate, and CAIC; we note up front that this multi-objective rule uses the same $2024$--$2026$ window later employed for VaR evaluation, and is therefore not a strictly held-out criterion (a clean held-out re-selection on a $2022$--$2023$ validation slice that does not overlap the OoS window picks $K^\star = 3$ by held-out log-likelihood, BIC, HQC, and CAIC; AIC picks $K^\star = 6$; held-out KS picks $K^\star = 9$; none picks $K = 18$, see Appendix~\ref{sec:supp_misc}). The cost of moving from $K^\star = 3$ to $K = 18$ is the $25.2$-nat held-out-likelihood penalty above; the gain is documented in Tables~\ref{tab:model_comparison} and \ref{tab:cross_ticker}. The complete per-$K$ panel ($K \in \{3, 6, 9, 12, 15, 18, 21\}$) and the full held-out-validation sweep are reported in Appendix~\ref{sec:supp_sensitivity} (held-out details in Appendix~\ref{sec:supp_misc}).

\subsection{Six-Generator Comparison (Pipeline~A)}
\label{sec:model_comparison}

This subsection operates under Pipeline~A (Figure~\ref{fig:pipeline_schematic}, top row): every generator is fit independently to the SPY return series, no cross-asset coupling. Table~\ref{tab:model_comparison} reports the IS and OoS validation results for the benchmark generators of Section~\ref{sec:benchmarks} together with the four CHMM emission variants at the headline operating point $K = 18$ and a CHMM-N reference row at the held-out-likelihood choice $K^\star = 3$. The headline panel covers one representative from each benchmark tier: a non-parametric distributional ceiling (i.i.d.\ bootstrap, \citealp{politis1994stationary}), a Gaussian negative control, a Laplace i.i.d.\ comparator, the workhorse GARCH(1,1) volatility-clustering baseline of \citet{bollerslev1986generalized}, the heavy-tailed GARCH(1,1)-$t$ extension of \citet{bollerslev1987conditionally}, and the regime-switching MS-GARCH foil of \citet{haas2004new}. The full extended GARCH-family panel (EGARCH, GJR-GARCH, HAR-RV, MS-GARCH $K=3$), the semi-Markov CHMM foil, and the deep-generative QuantGAN baseline (\citealp{wiese2020quantgan}) are reported in Appendix~\ref{sec:extended_baselines}; the headline conclusions of this section are unchanged under that extended panel.

\begin{table}[t]
\centering
\caption{\textbf{Headline generator comparison on SPY}. $1{,}000$ simulated paths, $\alpha = 0.05$; IS $T = 2{,}516$ (2014-01-03 to 2024-01-03), OoS $T = 572$ (2024-01-04 to 2026-04-20). KS = Kolmogorov--Smirnov pass rate; Kurt = mean simulated excess kurtosis (observed $7.68$ IS, $5.29$ OoS); ACF-MAE $|G_t|$ / $G_t$ = mean absolute error of the absolute-return / raw-return ACF over $252$ lags; CRPS = OoS sample continuous-ranked-probability score (mean over $T_{\text{OoS}}$, $1{,}000$ paths). ``--'' marks rows not in the cached CRPS archive (no material expected difference from the reported CHMM rows, which cluster within $0.002$ DM). MS-GARCH ($K=2$) is the foil of \citet{haas2004new}; the extended-GARCH panel (EGARCH, GJR-GARCH, HAR-RV, MS-GARCH $K=3$) and the semi-Markov CHMM foil are in Appendix~\ref{sec:extended_baselines}.}
\label{tab:model_comparison}
\small
\begin{tabular}{l cc cc cc c}
\toprule
& \multicolumn{2}{c}{KS (\%) $\uparrow$} & \multicolumn{2}{c}{Kurtosis} & \multicolumn{2}{c}{ACF-MAE $\downarrow$} & CRPS OoS \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5} \cmidrule(lr){6-7}
Model & IS & OoS & IS & OoS & $|G_t|$ & $G_t$ & $\downarrow$ \\
\midrule
\textit{Observed}                    & --   & --   & $7.68$  & $5.29$  & --     & --     & --       \\
Bootstrap                            & $99.7$ & $92.1$ & $7.24$  & $6.81$  & $0.0628$ & $0.0235$ & $1.0398$ \\
Gaussian i.i.d.                      & $0.0$  & $1.0$  & $-0.01$ & $-0.03$ & $0.0627$ & $0.0236$ & $1.0611$ \\
Laplace i.i.d.                       & $99.1$ & $88.5$ & $2.95$  & $2.91$  & $0.0628$ & $0.0236$ & $1.0386$ \\
GARCH(1,1)                           & $23.4$ & $60.8$ & $7.06$  & $2.96$  & $0.0485$ & $0.0243$ & $1.0440$ \\
GARCH(1,1)-$t$                       & $57.3$ & $80.8$ & $15.13$ & --      & $\mathbf{0.0316}$ & $\mathbf{0.0173}$ & --       \\
MS-GARCH ($K=2$)                     & $27.7$ & $38.7$ & $4.73$  & --      & $0.0367$ & $0.0173$ & --       \\
CHMM-N ($K^\star = 3$)               & $89.7$ & $80.1$ & $3.86$  & --      & $0.0467$ & $0.0240$ & --       \\
ML HSMM-N ($K^\star = 3$)            & $98.4$ & $\mathbf{91.0}$ & $3.46$  & $3.38$  & $0.0629$ & $0.0236$ & --       \\
\textbf{CHMM-N ($K = 18$)}           & $94.1$ & $81.8$ & $5.04$  & $4.44$  & $0.0509$ & $0.0237$ & $\mathbf{1.0384}$ \\
\textbf{CHMM-t ($K = 18$)}           & $95.6$ & $\mathbf{85.7}$ & $14.35$ & $10.71$ & $0.0549$ & $0.0234$ & $1.0399$ \\
\textbf{CHMM-t pen.\ ($\lambda = 20$)} & $95.0$ & $85.8$ & $\mathbf{8.56}$ & $\mathbf{7.07}$ & $0.0542$ & $0.0235$ & --       \\
\textbf{CHMM-L ($K = 18$)}           & $93.6$ & $80.8$ & $6.63$  & $6.18$  & $0.0567$ & $0.0234$ & $1.0400$ \\
\textbf{CHMM-GED ($K = 18$)}         & $\mathbf{95.2}$ & $84.3$ & $5.15$ & $4.80$ & $0.0548$ & $0.0234$ & --       \\
\bottomrule
\end{tabular}
\end{table}

The headline finding is in the bottom four rows.
At $K = 18$, CHMM-N attains $94.1\%$ IS KS and $81.8\%$ OoS KS (Table~\ref{tab:model_comparison}), substantially above GARCH ($23.4\%$ IS KS) and within the band of the bootstrap distributional ceiling ($99.7\%$ IS / $92.1\%$ OoS); the absolute-return ACF-MAE of $0.0509$ is essentially on par with GARCH ($0.0485$), the empirical instance of the spectral identity~\eqref{eq:acf_normalised}: with up to $K-1 = 17$ non-unit eigenvalues at the operating point, the mixture-of-eigenvalues sum reproduces the slow absolute-return ACF that the \citet{ryden1998stylized} $K = 2$--$3$ Gaussian setup could not recover, inside a standard HMM.
The raw-return ACF-MAE column closes the third stylized fact: every CHMM row sits at $0.0234$--$0.0240$, statistically indistinguishable from the i.i.d.\ baselines ($0.0235$--$0.0243$) that have zero linear ACF by construction. Linear autocorrelation is therefore reproduced as a non-binding consequence of the regime structure, not enforced by an explicit AR mechanism.
The bootstrap (at $0.0628$ on $|G_t|$) and the i.i.d.\ baselines all sit far above the temporal floor on the volatility-clustering metric.

CHMM-N at the held-out-likelihood reference $K^\star = 3$ remains a credible operating point: $89.7\%$ IS KS, $80.1\%$ OoS KS, $|G_t|$ ACF-MAE $0.0467$, raw-$G_t$ ACF-MAE $0.0240$. The cost of staying at $K^\star = 3$ is a roughly $5$ percentage point drop in IS KS and a tighter kurtosis residual ($3.86$ vs $5.03$ at $K = 18$). The benefit is the $25.2$-nat held-out-likelihood gain documented in Section~\ref{sec:k_selection_results}. For risk-management consumers who require minimum model complexity the $K^\star = 3$ row is the recommended default; for synthetic-data consumers who require the strongest joint match on KS and tail moments the $K = 18$ row is the operating point. A six-fold rolling-origin walk-forward (5-year train / 1-year test, Appendix~\ref{sec:supp_misc}, Table~\ref{tab:walkforward}) attains median OoS KS $67.7\%$ (IQR $[8.2, 75.0]$) at $K = 18$ and $62.1\%$ ($[7.2, 78.4]$) at $K^\star = 3$, against the single-window OoS KS of $81.8$ and $80.1\%$ in Table~\ref{tab:model_comparison}: the single-window number sits at the upper end of the walk-forward distribution, with two stress folds (W2 COVID, W4 the 2022 rate-hike onset) below $10\%$ and four non-stress folds in $61$--$83\%$. The headline ranking ($K = 18 \ge K^\star = 3$ on KS; both within $0.005$ of each other on $|G_t|$ ACF-MAE) is window-robust on the four non-stress folds; on stress folds all CHMM rows drop sharply, consistent with the stationarity-scope limit that Section~\ref{sec:cross_asset_univariate} flagged for NVDA / JPM.

\paragraph{ML HSMM at $K^\star = 3$ as a semi-Markov reference.}
We replace the geometric sojourn distribution of the Markov chain with an explicit per-state truncated discrete Pareto distribution and re-fit by maximum likelihood through Yu's (2010) explicit-duration forward-backward over augmented $(s_t, d_t)$ pairs ($D_{\max} = 200$, off-diagonal transitions, Gaussian emissions; full derivation and the complementary plug-in semi-Markov foil in Appendix~\ref{sec:extended_baselines}). At $K^\star = 3$, ML HSMM-N attains $98.4\%$ IS KS and $91.0\%$ OoS KS (the highest OoS KS of any row in Table~\ref{tab:model_comparison}) at simulated kurtosis $3.46$ and $|G_t|$ ACF-MAE $0.0629$. The result is a single-metric gain rather than a uniform improvement: ML HSMM-N wins on KS but undershoots simulated kurtosis below CHMM-N at the same $K$ ($3.46$ vs $3.86$, against observed $7.68$ IS), and the absolute-return ACF-MAE matches the i.i.d.\ baseline level ($0.0629$ vs $0.0628$ for the bootstrap and $0.0628$ for the Laplace i.i.d.) rather than CHMM-N's $0.0467$. The natural diagnosis is that the explicit-duration Pareto sojourn at $K = 3$ concentrates probability mass in long-sojourn states and reduces both tail-state realisation frequency (kurtosis down) and within-window absolute-return correlation (ACF-MAE up to baseline); under this reading, the explicit-duration scaffold buys distributional fidelity at $K = 3$ at the cost of the temporal mechanism that CHMM at $K \ge 3$ recovers through the non-unit spectrum of $\mathbf T$, and the body framing is best read as ``two complementary scaffolds at the same Markov backbone, with different trade-offs across the three Cont stylized facts'' rather than a strict comparison. The same Yu (2010) construction at $K = 18$ collapses to a near-degenerate local optimum on $T_{\text{IS}} = 2{,}516$ (IS KS $0.8\%$, simulated kurtosis $3.65$): the joint $K^2 + KD_{\max}$ free-parameter count exceeds the effective IS sample size, so the higher-$K$ ML HSMM is best read as a companion-paper direction at longer training windows or under a constrained sojourn parameterisation. Heavy-tailed emissions and higher-$K$ ML HSMM are companion-paper directions on this scaffold.

The CHMM emission variants land at distinct points on the kurtosis axis without retuning $K$ or the transition structure: CHMM-N undershoots ($5.0$ IS vs.\ observed $7.7$), unpenalised CHMM-t overshoots ($14.4$, the per-state $\nu_k$ ECM lower-bracket pinning artefact discussed in Section~\ref{sec:discussion}), CHMM-L matches cleanly ($6.6$, within $1.1$ units of observed), CHMM-GED sits adaptively between the closed-form anchors at $5.2$, and the penalised CHMM-t with $1/\nu_k$ shrinkage at $\lambda = 20$ (Section~\ref{sec:discussion}, Appendix~\ref{sec:supp_misc}) is the cleanest single-row match at $8.56$ IS / $7.07$ OoS while retaining the same KS pass rate as unpenalised CHMM-t to within $1$ percentage point. CHMM-GED also attains the highest IS Kolmogorov-Smirnov pass rate among the four variants ($95.2\%$) at the same parameter count as CHMM-t. All four variants match each other on KS pass rates to within $2$ percentage points IS and within $5$ percentage points OoS; on the absolute-return ACF-MAE they sit within $0.006$ of each other and on the raw-return ACF-MAE within $0.0003$. The emission-family swap moves tail weight cleanly without disturbing the central or temporal fit.

\paragraph{The CHMM-GED $\hat p_k$ partition: data chooses Gaussian-bulk / Laplace-tail.}
CHMM-GED carries a per-state shape $p_k \in [p_{\min}, p_{\max}] = [0.5, 3.0]$ that nests Gaussian ($p = 2$) and Laplace ($p = 1$) as boundary cases (Section~\ref{sec:mstep}). The empirical fit on SPY IS at $K = 18$ partitions states bimodally (Figure~\ref{fig:p_hist}): thirteen states attain the upper bracket at $p_k = 3.0$ (Gaussian-or-beyond shape), one state lands at $p_k = 1.6$, and four states cluster in the Laplace regime $p_k \in [0.86, 1.24]$. The smallest fitted $p_k$ is $0.86$, well clear of every $p_{\min}$ tested in the bracket sweep (Appendix~\ref{sec:nu_diagnostics}), so the lower bracket is non-binding. The four Laplace-shape states are concentrated in the high-volatility tail of the state ordering: ranks $13$, $15$, and $18$ by variance-equivalent $\sigma$ all sit near $\hat p_k = 1$. This is the data-driven analog of a hand-classified Gaussian-bulk / Laplace-tail hybrid, found by ECM rather than imposed a priori. The partition replicates across $10$ Monte Carlo seeds (every seed yields the same partition counts to within one state) and across the cross-asset universe (Appendix~\ref{sec:supp_robustness}); we read it as the most distinctive empirical finding of the four-emission scaffold.

\begin{figure}[t]
\centering
\begin{subfigure}[b]{0.49\textwidth}\centering
\includegraphics[width=\textwidth]{figs/Fig-p-Histogram-a.pdf}
\caption{Histogram of fitted $\hat p_k$.}
\end{subfigure}\hfill
\begin{subfigure}[b]{0.49\textwidth}\centering
\includegraphics[width=\textwidth]{figs/Fig-p-Histogram-b.pdf}
\caption{$\hat p_k$ vs.\ volatility rank.}
\end{subfigure}
\caption{\textbf{CHMM-GED per-state shape $\hat p_k$ partition} ($K = 18$, SPY IS, seed $20260420$). Panel~(a): histogram of the eighteen fitted $\hat p_k$ values; the dashed reference lines mark Laplace ($p = 1$) and Gaussian ($p = 2$). The bimodal partition is visible at the upper bracket ($p_k = 3.0$, Gaussian-or-beyond shape) and around the Laplace shape. Panel~(b): $\hat p_k$ versus state rank ordered by variance-equivalent $\sigma$ (rank $1$ is the calmest state, rank $18$ the highest-volatility state); the Laplace-shape cluster sits at the high-volatility ranks. The partition replicates across $10$ seeds and across the six-ticker cross-asset panel (Appendix~\ref{sec:supp_robustness}).}
\label{fig:p_hist}
\end{figure}

The OoS sample-CRPS column tightens the picture: CHMM-N at $K = 18$ attains the lowest mean OoS CRPS in the panel ($1.0384$), and CHMM-t, CHMM-L, and the Laplace i.i.d.\ baseline are within $0.002$ and statistically indistinguishable from CHMM-N under a Diebold-Mariano test (HAC variance, two-sided $p > 0.45$). On a proper scoring rule the CHMM family ties or beats every benchmark, ruling out the concern that the headline KS finding is a non-proper-metric artefact. No single benchmark dominates every metric: the i.i.d.\ bootstrap is the strongest distributional baseline but the worst on temporal fidelity ($|G_t|$ ACF-MAE $0.0628$); GARCH(1,1)-$t$ wins the volatility-clustering metric ($0.0316$) but fails KS ($57.3\%$ IS); the CHMM at $K = 18$ occupies the joint-fit corner. Headline pass rates are stable under multi-seed and KS-power-calibration controls (ten-seed std $\le 3.6$pp on OoS KS; KS-power positive control passes at $90\%$ asymptotic, $98$--$99\%$ block-bootstrap; Appendix~\ref{sec:supp_robustness}, \ref{sec:ks_power}, \ref{sec:ks_block_bootstrap}); the seed-to-seed standard deviation refers to ten alternative global seeds, not within-path SEs at the headline single-seed run.

\begin{figure}[t]
\centering
\begin{subfigure}[b]{0.49\textwidth}
\centering
\includegraphics[width=\textwidth]{figs/Fig-3-IS-Comparison-K18-a.pdf}
\caption{Marginal density.}
\end{subfigure}
\hfill
\begin{subfigure}[b]{0.49\textwidth}
\centering
\includegraphics[width=\textwidth]{figs/Fig-3-IS-Comparison-K18-b.pdf}
\caption{Tail Q-Q plot.}
\end{subfigure}\\[0.5em]
\begin{subfigure}[b]{0.49\textwidth}
\centering
\includegraphics[width=\textwidth]{figs/Fig-3-IS-Comparison-K18-c.pdf}
\caption{ACF of raw $G_t$.}
\end{subfigure}
\hfill
\begin{subfigure}[b]{0.49\textwidth}
\centering
\includegraphics[width=\textwidth]{figs/Fig-3-IS-Comparison-K18-d.pdf}
\caption{ACF of $|G_t|$.}
\end{subfigure}
\caption{\textbf{IS CHMM-N validation on SPY} ($K = 18$, $1{,}000$ simulated paths). Panel~(a): marginal density of the annualized excess log return $G_t$; gray histogram $=$ observed IS series, blue curve $=$ one simulated path. Panel~(b): tail Q-Q plot at $200$ quantile levels; blue points $=$ simulated pooled over paths, black dashed $=$ identity. Panel~(c): ACF of raw $G_t$ at lags 1--252 (linear autocorrelation); orange dashed $=$ observed, green $=$ mean across sim paths, shaded band $=$ 10th--90th percentile across sim paths, dashed grey $=$ $99\%$ Bartlett confidence band. Panel~(d): ACF of $|G_t|$ on the same window (volatility clustering); same colour conventions. The 2x2 layout mirrors the empirical-only Figure~\ref{fig:stylized_facts}: panels (a/b) verify marginal fidelity, panels (c/d) the two ACF-based stylized facts.}
\label{fig:is_comparison}
\end{figure}

\subsection{Cross-Ticker Generalization (Pipeline~A)}
\label{sec:cross_asset_univariate}

Still under Pipeline~A: each ticker is fit independently, no cross-asset coupling.
Table~\ref{tab:cross_ticker} reports the penalised CHMM-t IS and OoS KS pass rates across the five additional tickers (NVDA, JNJ, JPM, AAPL, QQQ), each fit independently at $K = 18$ under the $1/\nu_k$ shrinkage prior at $\lambda = 20$. CHMM-t is the natural generalization vehicle here because it carries the strongest OoS KS in the SPY headline panel ($85.6\%$, against $84.7\%$ for CHMM-N and $81.6\%$ for CHMM-L) and supplies the per-state heavy tails that the outlier-prone single-name tickers (NVDA, JNJ) require, and the penalised variant tames the per-state $\nu_k$ ECM lower-bracket pinning that drives the unpenalised cross-ticker kurtosis overshoots (Appendix~\ref{sec:cross_ticker_per_family_kurt}). Per-family $K$-sensitivity for SPY across all four emission variants and per-ticker price-level metrics across all four families are in Appendix~\ref{sec:multi_emission_sensitivity} (per-ticker price-level figures in Appendix~\ref{sec:price_sim_oos_appendix}).
The univariate fidelity persists across the panel on IS KS ($95.8\%$ to $99.8\%$). On OoS, JNJ, AAPL, and QQQ generalise cleanly ($92$--$95\%$), while NVDA ($53.8\%$) and JPM ($53.8\%$) show substantial OoS degradation, a stationarity artefact: the IS-fixed transitions and emissions cannot capture the artificial-intelligence (AI) driven NVDA regime or the rate-repricing cycle for financials in the $2024$--$2026$ holdout.
We acknowledge the JPM and NVDA OoS cliffs as stationarity-scope limitations rather than model-selection issues; the pattern persists at every $K$ in the sweep, and we discuss the operational implication (periodic refit at deployment) in Section~\ref{sec:discussion}.
Table~\ref{tab:cross_ticker} reports the recommended operational variant, the penalised CHMM-t with $1/\nu_k$ shrinkage at $\lambda = 20$. The $1/\nu_k$ penalty addresses the per-state ECM lower-bracket pinning mechanism documented for SPY in Section~\ref{sec:nu_diagnostics}: under the unpenalised ECM the CHMM-t produces simulated-kurtosis blow-ups on the heavy-tailed defensives (NVDA $46.4$, JNJ $99.7$, JPM $18.8$ against observed $5.5$, $9.0$, $7.6$; full unpenalised cross-ticker numbers in Appendix~\ref{sec:cross_ticker_per_family_kurt}, Table~\ref{tab:cross_ticker_kurt_per_family}, alongside the CHMM-N, CHMM-L, CHMM-GED rows that do not exhibit the blow-up). At $\lambda = 20$ the simulated kurtosis drops to within $0.5$ kurtosis units of observed on SPY, NVDA, AAPL, and JPM, and to within $3.3$ on JNJ; the unconditional KS pass rates and $|G_t|$ ACF-MAE move by less than $1.5$ percentage points and $0.001$ respectively across the panel.

\begin{table}[t]
\centering
\caption{\textbf{Cross-ticker generalization, penalised CHMM-t at $K = 18$ ($\lambda = 20$)}. Each ticker fit independently under the $1/\nu_k$ shrinkage prior; $1{,}000$ simulated paths, $\alpha = 0.05$, seed $= 20260420$. Observed kurtosis is the IS sample value. Unpenalised CHMM-t numbers (with the per-state ECM lower-bracket pinning kurtosis blow-ups) and the per-family kurtosis comparison across CHMM-N, CHMM-t, CHMM-L, CHMM-GED are in Appendix~\ref{sec:cross_ticker_per_family_kurt}.}
\label{tab:cross_ticker}
\small
\begin{tabular}{l cc c c c}
\toprule
& \multicolumn{2}{c}{KS (\%) $\uparrow$} & \multicolumn{2}{c}{Kurtosis} & ACF-MAE \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5}
Ticker & IS & OoS & Obs & Sim & $\downarrow$ \\
\midrule
SPY  & $95.8$ & $85.0$ & $7.7$ & $8.01$  & $0.0541$ \\
NVDA & $99.5$ & $53.8$ & $5.5$ & $5.23$  & $0.0473$ \\
JNJ  & $99.8$ & $92.3$ & $9.0$ & $12.28$ & $0.0334$ \\
JPM  & $97.8$ & $53.8$ & $7.6$ & $7.70$  & $0.0457$ \\
AAPL & $99.1$ & $95.1$ & $3.2$ & $2.84$  & $0.0418$ \\
QQQ  & $96.1$ & $91.8$ & $3.4$ & $2.88$  & $0.0569$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\begin{subfigure}[b]{0.49\textwidth}
\centering
\includegraphics[width=\textwidth]{figs/Fig-4-OoS-Validation-K18-a.pdf}
\caption{OoS density fan.}
\end{subfigure}
\hfill
\begin{subfigure}[b]{0.49\textwidth}
\centering
\includegraphics[width=\textwidth]{figs/Fig-4-OoS-Validation-K18-b.pdf}
\caption{KS $p$-value histogram.}
\end{subfigure}\\[0.5em]
\begin{subfigure}[b]{0.49\textwidth}
\centering
\includegraphics[width=\textwidth]{figs/Fig-4-OoS-Validation-K18-c.pdf}
\caption{OoS ACF of raw $G_t$.}
\end{subfigure}
\hfill
\begin{subfigure}[b]{0.49\textwidth}
\centering
\includegraphics[width=\textwidth]{figs/Fig-4-OoS-Validation-K18-d.pdf}
\caption{OoS ACF of $|G_t|$.}
\end{subfigure}
\caption{\textbf{OoS CHMM-N validation on SPY} ($K = 18$, $1{,}000$ paths, $T_{\text{OoS}} = 572$). Panel~(a): OoS return-density fan; blue thin curves $=$ simulated densities, orange thick curve $=$ observed OoS density. Panel~(b): histogram of the $1{,}000$ OoS KS $p$-values against the observed OoS series with the $\alpha = 0.05$ threshold (orange dashed). Panel~(c): ACF of raw $G_t$ on the OoS window (linear autocorrelation); orange dashed $=$ observed, green $=$ mean across sim paths, shaded band $=$ 10th--90th percentile, dashed grey $=$ $99\%$ Bartlett band (wider than IS because $T_{\text{OoS}}$ is shorter). Panel~(d): ACF of $|G_t|$ on the OoS window (volatility clustering); same colour conventions. The 2x2 layout parallels the IS comparison Figure~\ref{fig:is_comparison}.}
\label{fig:oos_validation}
\end{figure}

\subsection{Cross-Asset Dependence (Pipeline~B): Student-t Copula}
\label{sec:cross_asset}

This is the only Pipeline~B subsection in the main text (Figure~\ref{fig:pipeline_schematic}, bottom row): we reuse the per-asset CHMM-N marginals from \S\ref{sec:cross_asset_univariate} and add a rank-based Student-$t$ dependence layer (Section~\ref{sec:cross_asset_methods}, full derivation in Appendix~\ref{sec:cross_asset_supp}). Profile MLE on the six-asset universe (SPY, NVDA, JNJ, JPM, AAPL, QQQ; $T_{\text{IS}} = 2{,}516$, $200$ simulated paths) selects $\nu^\star = 6$ on the unit-spaced grid $\nu \in [4, 12]$ with a Wilks $95\%$ profile-LL CI of $[6, 7]$ (cutoff $-1.92$ profile-LL units below the optimum; Appendix~\ref{sec:supp_cross_asset}), consistent with values typically reported for equity indices \citep{demarta2005tcopula, mcneil2015quantitative}.

\begin{table}[t]
\centering
\caption{\textbf{Cross-asset Student-t copula on CHMM-N marginals} (Pipeline~B; $\nu^\star = 6$ by profile MLE; $200$ paths). Per-asset KS pass rate at $\alpha = 0.05$ (rank reordering preserves marginals exactly, so the per-asset KS column is a sanity check after dependence injection rather than a substitute for Table~\ref{tab:cross_ticker}). The lower block reports correlation reproduction averaged over paths: Frobenius norm of $\hat\Sigma_{\text{sim}} - \hat\Sigma_{\text{obs}}$ and off-diagonal mean absolute error. Comparison against SIM, the Gaussian copula, and a truncated C-vine on the same marginals is in Appendix~\ref{sec:supp_cross_asset}; the Student-t copula is the best of the four on the headline IS off-diagonal MAE ($0.027$ vs.\ $0.030$ Gaussian, $0.068$ truncated C-vine, $0.076$ SIM).}
\label{tab:cross_asset}
\small
\begin{tabular}{l cc}
\toprule
& \multicolumn{2}{c}{KS (\%) $\uparrow$} \\
\cmidrule(lr){2-3}
Ticker & IS & OoS \\
\midrule
SPY   & $96.0$  & $79.5$ \\
NVDA  & $98.5$  & $53.5$ \\
JNJ   & $99.0$  & $97.5$ \\
JPM   & $99.5$  & $48.0$ \\
AAPL  & $99.0$  & $93.5$ \\
QQQ   & $95.5$  & $92.0$ \\
\midrule
Mean   & $97.9$ & $77.3$ \\
Median & $98.8$ & $85.8$ \\
\midrule
\multicolumn{3}{l}{\textit{Correlation reproduction (averaged over $200$ paths)}} \\
Frobenius IS         & \multicolumn{2}{c}{$0.183$} \\
Off-diag MAE IS      & \multicolumn{2}{c}{$0.027$} \\
Frobenius OoS        & \multicolumn{2}{c}{$1.507$} \\
Off-diag MAE OoS     & \multicolumn{2}{c}{$0.209$} \\
\bottomrule
\end{tabular}
\end{table}

Rank reordering preserves each fitted CHMM marginal exactly, so the per-asset KS column is a sanity check after dependence injection rather than a substitute for Table~\ref{tab:cross_ticker}. The headline dependence-layer result is the off-diagonal correlation MAE: $0.027$ on IS for the Student-$t$ copula, against $0.030$ Gaussian, $0.068$ truncated C-vine, and $0.076$ SIM factor (Appendix~\ref{sec:cross_asset_supp}); the copula advantage is intrinsic to rank reordering rather than specific to the marginal model. On OoS the off-diagonal MAE rises to $0.209$ and the Gaussian and Student-$t$ copulas are statistically indistinguishable. The degradation localises to a single-name regime shift in the JNJ-equity-factor relationship: a per-pair breakdown (Appendix~\ref{sec:cross_asset_supp}) shows three JNJ pairs with $|\Delta| > 0.48$ on OoS, while extending the universe with GLD (a non-equity commodity) reduces the OoS off-diagonal MAE to $0.179$ (Appendix~\ref{sec:non_us_asset_supp}); a quarterly rolling-window refit reduces it further to $0.185$ on the original six-asset universe (Appendix~\ref{sec:rolling_copula_supp}).
