A continuous hidden Markov model trained by Baum-Welch with quantile-based initialization reproduces the three Cont stylized facts of daily equity returns simultaneously at moderate state resolution; the closed-form mixture-of-eigenvalues identity for the absolute-return ACF recasts the Ryd\'{e}n et al.\ low-$K$ failure as a $K$-rank statement on $\mathbf T$. Three emission variants share the same scaffold and land at three distinct points on the kurtosis axis: the Gaussian CHMM-N undershoots, the Student-t CHMM-t overshoots and is the per-state heavy-tailed choice for tail-conditional consumers, and the Laplace CHMM-L matches observed kurtosis cleanly without a shape parameter. The M-step swap is the only architectural difference across the three.

The headline quantitative findings on SPY are $93.8\%$ in-sample and $84.2\%$ out-of-sample Kolmogorov--Smirnov pass rates, an absolute-return ACF mean absolute error within $0.003$ of GARCH(1,1), a clean Kupiec pass on Value-at-Risk at both $1\%$ and $5\%$, and Value-at-Risk and Expected-Shortfall envelopes that bracket the observed historical values on both windows. Substantial OoS degradation on NVDA and JPM reflects the well-known stationarity scope limit on individual financial time series and motivates periodic refit in operational deployment, in line with standard practice for time-series generators in this domain.

Multi-asset synthesis composes cleanly with the rank-based Student-t copula on top of the CHMM-N marginals: profile MLE selects $\nu^\star = 6$, and the resulting cross-asset construction reproduces the off-diagonal correlation matrix on six SPY-member assets with a mean absolute error of $0.027$, against $0.076$ for a Single Index Model factor baseline on the same marginals.

Skew-heavy-tailed emissions, explicit-duration semi-Markov sojourns, and operational regime-conditional VaR are companion-paper directions; a truncated C-vine on the same six-asset universe does not improve over the elliptical copulas (Appendix~\ref{sec:cvine_supp}), so the natural multi-asset follow-up is scaling to larger $d$ via untruncated regular vines or factor copulas.

\paragraph{Data and code availability.}
The simulation scripts and data to reproduce the paper are available under an MIT license at \url{https://github.com/altashly1/CHMM-paper}.
The companion Julia package \texttt{CHMM-Model.jl} is released alongside this paper at \url{https://github.com/altashly1/CHMM-Model}; the public API listed in Appendix~\ref{sec:supp_chmm_api} regenerates every table and figure from a single seed root under Julia $\geq 1.12$.

\paragraph{Conflict of interest.}
The authors declare no competing interests.

\paragraph{Author contributions.}
J.V.\ directed the study.
A.A.\ developed the continuous model, implemented the Baum-Welch algorithm, conducted the empirical analysis, and generated all figures and tables.
C.J.\ contributed to methodology review and validation.
All authors edited and reviewed the final manuscript.
