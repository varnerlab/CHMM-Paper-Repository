# JoFE Major-Revision Plan

Ordered by the referee's own priority signal. Each item is labeled **[text]** (edit in this repo), **[code]** (re-run in `CHMM-Model`), or **[both]**.

## Tier 1 — must-fix

1. **M3 — Filter-based regime-conditional VaR** *(single most important)*. **[both]**
   - Code: replace Viterbi smoother with forward-only filter $P(s_t\mid r_{1:t})$ using IS-fit params; re-run Kupiec + Christoffersen on filter-derived quantile; retain Viterbi variant as IS goodness-of-fit diagnostic only.
   - Paper: rewrite §var_backtest, Eq. (9), and headline claim in abstract/introduction/discussion/conclusion.

2. **M1 — Reposition novelty** as integrative evaluation + regime-conditional VaR, not methodological innovation. **[text]**
   - Rewrite intro contribution list, related_work, and the conclusion's "resolving Rydén" framing.

3. **M8 — Clean K=18 selection.** **[both]**
   - Pick K on a pre-OoS validation slice (e.g. 2022–23) or walk-forward log-likelihood; evaluate 2024–26 once.
   - Rewrite §sensitivity_appendix and Table 3/4 references.

4. **M4 — Broaden empirical base.** **[both]**
   - Add MSCI / DAX / Nikkei / FTSE; rolling-origin OoS; second frequency (weekly).
   - Extend results.tex + cross_asset_appendix.

5. **M7 — GARCH competitor suite.** **[both]**
   - Add EGARCH, GJR, GARCH-t, HAR-RV / Realized GARCH, FIGARCH; expand MS-GARCH beyond single 2-regime spec.
   - Paper: baselines_appendix + main panel tables.

## Tier 2 — substantive but smaller

6. **M2 — Block-bootstrap KS recalibration.** **[both]** Report mean p-value + distribution, not just pass-rate.
7. **M5 — Bootstrap null for LR_ind at n=572, α=0.01.** **[both]** Temper "passes cleanly" language throughout.
8. **M6 — Explicit-duration forward–backward MLE for SM-CHMM** (Yu 2010). **[both]** At least on a subset; compute SM conditional-VaR instead of deferring.
9. **M9 — Skew-t / skew-Laplace emission ablation.** **[both]**
10. **M10 — Hierarchical / shrunk prior on $\nu_k$** to cure CHMM-t kurtosis overshoot. **[both]**

## Tier 3 — minor cleanup (all in this repo)

11. Minor 1 — abstract "LR_uc = 0.01 tie" language. **[text]**
12. Minor 2 — unit audit of annualized moments in §descriptive and §data (2.19 vs. ≈17% flagged). **[text]**
13. Minor 3 — Table 3 missing OoS AD cells for Bin-T NJ / Block-BS / GRU. **[text]**
14. Minor 4 — Table 5 MMD bandwidth: fix to observed-sample heuristic, not per-generator. **[both]**
15. Minor 5 — §cross_asset off-diagonal MAE definition (15 vs. 30 entries). **[text]**
16. Minor 6 — add K_disc=13 centroid ablation row to isolate emission vs. bin-count effect. **[both]**
17. Minor 7 — reconcile "flat CHMM-t smallest MMD" vs. SM-CHMM-L's 4.0e-5. **[text]**
18. Minor 8 — add citations: Timmermann (2000), Psaradakis & Sola (1998), Augustyniak (2014), Andersen & Bollerslev (1997), Ding–Granger–Engle (1993), Maheu & McCurdy (2004). **[text]**
19. Minor 9 — "Rydén" diacritic consistency pass. **[text]**
20. Minor 10 — 10–20-seed Monte Carlo summary for headline pass-rate and VaR-LR statistics. **[both]**

## Working order for this session

- **Step 1 — DONE (2026-04-24).** M1 repositioning. Softened "resolving Rydén" / "directly challenges" / "unnecessary" language and reframed novelty as integrative evaluation in the following locations:
  - `paper.tex` abstract — dropped "without any jump augmentation or return-space discretization" and added an explicit "integrative evaluation rather than a new estimator" framing.
  - `sections/introduction.tex` L10–12 — replaced "We show that these complexities are unnecessary" + four-axis "departs from" list with "combine on a single shared scaffold" language, added `peel2000robust, liu1995ml, abantovalle2017svm` antecedents, added "the novelty claim attaches to the integration and to the three-axis evaluation, not to any individual axis taken alone".
  - `sections/introduction.tex` L16 — softened "The negative result of Rydén is a low-K artifact" to "consistent with broader regime-switching evidence that ... is specific to low state resolution", added HSMM / jump-augmented "complements rather than replaces" framing.
  - `sections/related_work.tex` L16–17 — replaced "resolves the Rydén limitation through a third route" with "consistent with this broader regime-switching literature ... complements ... rather than superseding", anchored the integrative contribution on the unified EM scaffold + twelve-generator panel + regime-conditional VaR test.
  - `sections/conclusion.tex` L6–8 — dropped "directly challenges the influential conclusion" and "artifact of insufficient state resolution", replaced with "consistent with the broader regime-switching literature ... complements ... rather than superseding".
  - `sections/discussion.tex` — retitled §"Resolving the Rydén et al. Limitation" → §"Replicating the Slow-ACF Behaviour at Higher State Resolution"; retitled §"Why Jumps Are Unnecessary" → §"Why the Jump Correction Is Not Required in This Scaffold"; softened "directly contradicting" to "consistent with ... rather than a contradiction of it"; softened the "distributional low-K artefact" claim to "our K=2 replication indicates the binding low-K constraint is distributional rather than temporal".
  - `sections/sensitivity_appendix.tex` L456 — matched discussion softening.
  - Verified: no em dashes introduced; `pdflatex` compiles to 71 pages with no undefined references.

- **Step 2 — DONE (2026-04-24).** Minor cleanup that doesn't depend on re-running numerics (minors 1, 2, 5, 8, 9) plus a residual M1 cleanup in `sections/results.tex` L133.
  - **Minor 1 (LR_uc tie language):** `paper.tex` abstract and `sections/discussion.tex` L66 rewritten from "tie for the best-in-panel Kupiec" to "both sit at the coarse-grid floor ... corresponding to exact-target calibration rather than a resolved head-to-head ranking".
  - **Minor 2 (annualized moments convention):** `sections/results.tex` Table 1 caption now explicitly states that the "annualized" Mean and Std Dev rows are moments of the drift-convention $G_t = (1/\Delta t) \ln(P_t/P_{t-1}) - r_f$ with $\Delta t = 1/252$, and gives the $\sqrt{252}$-scaled volatility equivalent ($\approx 13.8\%$ IS, $\approx 12.5\%$ OoS) to resolve the "2.19 vs. 17%" flag.
  - **Minor 5 (off-diagonal MAE definition):** `sections/metrics_appendix.tex` now gives the explicit formula averaged over $d(d-1)/2 = 15$ unique upper-triangular entries (not double-counted); `sections/results.tex` L427 references the appendix at first use.
  - **Minor 8 (missing citations):** six new BibTeX entries added to `references.bib` (`timmermann2000moments`, `psaradakis1998finite`, `augustyniak2014maximum`, `andersen1997heterogeneous`, `ding1993long`, `maheu2004news`). Citations inserted in `sections/model.tex` MS-GARCH paragraph (Psaradakis-Sola, Augustyniak, Timmermann) and in `sections/related_work.tex` stylized-facts paragraph (Ding-Granger-Engle, Andersen-Bollerslev) and jump-diffusion paragraph (Maheu-McCurdy). Verified all six appear in `paper.bbl`.
  - **Minor 9 (Rydén diacritics):** audit showed all prose instances already use the proper `Ryd\'{e}n` form; no edits needed.
  - **M1 residual cleanup:** `sections/results.tex` L133 "the Ryd\'{e}n et al.\ limitation is resolved by moderate state resolution" softened to "the slow-ACF behaviour ... is available inside a standard HMM framework once the state resolution is moderate, complementing the HSMM and jump-augmented routes already in the literature".
  - Verified: no em dashes; full `pdflatex` + `bibtex` + two `pdflatex` build succeeds; 73 pages; all six new citations rendered in `paper.bbl`.

- **Step 3 — DONE (2026-04-24).** M3 framing rewritten around smoother-vs-filter split and M5 small-sample caveats inserted; `\revtodo{}` placeholders left where filter-based numbers must land.
  - **`\revtodo{}` macro added** in `paper.tex` preamble (red bold inline text) for easy grep-and-sweep once filter results land.
  - **§sec:conditional_var in `sections/var_backtest.tex` restructured.** Retitled to "Regime-Conditional Value-at-Risk: Smoother-Based Diagnostic and Filter-Based Backtest". Opening paragraph now explicitly distinguishes the smoother construction (Viterbi decode of the full OoS series; $\hat s_t$ depends on $r_{t+1:T}$, so it is an IS goodness-of-fit diagnostic on the regime structure, not a one-step-ahead backtest) from the filter construction (forward posterior $P(s_t \mid r_{1:t})$ under IS-fit parameters, mixture-of-emissions quantile, the operationally correct backtest). New equation `eq:filter_conditional_var` defines the filter-based VaR. The "Headline" paragraph is retitled "Smoother-diagnostic statistics" with an M5 caveat inserted that at $T_{\text{OoS}} = 572$ and $\alpha = 0.01$ only $\approx 5.7$ breaches are expected so small $\text{LR}_{\text{ind}}$ values should be read as "consistent with independence" rather than resolved rankings. "Passes Kupiec and Christoffersen cleanly" softened to "sits below the $\chi^2_1$ threshold" throughout.
  - **Table 4 (`tab:conditional_var`) expanded** from a 2-row-per-model panel (unconditional + conditional) to a 3-row-per-model panel (unconditional + smoother Viterbi + filter forward); the 24 filter cells carry `\revtodo{pending}` placeholders; caption rewritten to explain smoother-vs-filter, flag the M5 caveat, and mark the filter rows pending.
  - **Cross-section framing updated** in abstract (`paper.tex` L115), introduction (L28), discussion three-way split (L67), results `CHMM-N paragraph` leftover (`results.tex` L133), SM-CHMM conditional-VaR deferral paragraph, and the three-way-operational-split closing paragraph in `var_backtest.tex`. All now name the smoother as an IS regime-structure diagnostic and reserve the operationally binding claim for the pending filter-based rows.
  - **Residual "clean pass" language in `sections/var_backtest.tex` §SM-Ablation softened** to neutral numeric description plus an explicit "paper tempers 'passes cleanly' language in line with M5" note.
  - Verified: no em dashes; `pdflatex` compiles to 73 pages; all `\revtodo{}` placeholders render as red bold `[TODO (revision): ...]` markers visible in the PDF.

- **Step 4 — DONE (2026-04-24).** `revision-code-todo.md` drafted at the repo root, enumerating every code-repo deliverable.
  - Organised by referee priority: Tier 1 (M3, M1 text-only, M8, M4, M7), Tier 2 (M2, M5, M6, M9, M10), Tier 3 minors (4, 6, 10).
  - Each item specifies: (a) what to compute, (b) exact paper integration point (file, table, equation, row), (c) inputs (data, hyperparameters), and (d) outputs (CSV filename, which `\revtodo{...}` placeholder it replaces).
  - Global conventions section pins IS/OoS windows, $K = 18$ operating point, $N_{\text{paths}} = 1{,}000$, global seed `20260420`, and the drift-convention scaling of $G_t$, so the code-repo developer does not need to re-derive them from the paper.
  - Final section specifies a standardised deliverable format: CSVs into `CHMM-paper/results/robustness/` with descriptive filenames (`filter_var_backtest.csv` for M3, `index_panel.csv` for M4, etc.), plus an explicit rule that swapping in numbers must be paired with deletion of the matching `\revtodo{}` marker.

- **Step 5a — DONE (2026-04-24).** Paper-side residual minors (3, 7) + final M1/M3 consistency sweep of the full paper (before CSVs arrive from `CHMM-Model`).
  - **Minor 3 (Table 3 AD cells):** audit showed all OoS AD cells for Bin-T NJ, Block-BS, and GRU are already populated in `tab:model_comparison`; the referee was tripped up by a convoluted caption sentence. Rewrote the caption tail from "OoS AD for Bin-T NJ, Block-BS, and GRU is reported alongside OoS KS in the row (see Appendix ...)" to "Full per-generator panels for Bin-T NJ, Block-BS, and GRU are in Appendix~\ref{sec:baselines_appendix} (Tables ...)."
  - **Minor 7 (MMD reconciliation):** the `sections/var_backtest.tex` L125 claim that "SM-CHMM-L attains the smallest IS MMD of any row in the full panel ($4.0 \times 10^{-5}$)" contradicted the abstract / discussion claim that flat CHMM-t's $2.0 \times 10^{-5}$ is smallest ($2e{-5} < 4e{-5}$). Rewrote L125 to: "SM-CHMM-L attains the smallest IS MMD among the SM variants and improves on its flat CHMM-L counterpart ($4.0 \times 10^{-5}$, close to but not below flat CHMM-t's panel-leading $2.0 \times 10^{-5}$)".
  - **Appendix sweep:** grepped `sections/sensitivity_appendix.tex`, `sections/baselines_appendix.tex`, `sections/algorithms_appendix.tex`, `sections/cross_asset_appendix.tex`, `sections/metrics_appendix.tex` for residual "resolves the Rydén", "passes cleanly", "directly contradict", "Viterbi-decoded conditional" overclaims. None found; only Viterbi mentions in `algorithms_appendix.tex` are the algorithm presentation (neutral).
  - **M1 leftovers found and softened:** `sections/discussion.tex` L1 "without requiring jump augmentation" rewritten to "as an alternative to (rather than a replacement for)"; `sections/conclusion.tex` L3 "\emph{without} requiring jump augmentation, discretization, or any mechanism beyond the standard HMM framework" replaced with "within a standard HMM framework, as an alternative to (rather than a replacement for) the HSMM and jump-augmented routes"; `sections/results.tex` L132 "demonstrating that the CHMM reproduces volatility clustering \emph{without} a dedicated jump mechanism or conditional variance equation" replaced with "indicating that the CHMM reproduces volatility clustering from the transition-matrix eigenvalue spectrum alone, complementing the jump-process and conditional-variance-equation mechanisms available elsewhere in the literature"; `sections/related_work.tex` L21 "eliminates all three sources of complexity" replaced with "offers a simpler alternative on each of three axes", with explicit "modelling-cost comparison rather than a claim that the discrete-plus-jumps route is inferior" framing.
  - Verified: no em dashes; `pdflatex` builds twice cleanly to 74 pages; 15 `\revtodo{}` markers live (2 in `paper.tex` macro + abstract, 1 in `introduction.tex`, 1 in `discussion.tex`, 1 in `results.tex`, 10 in `var_backtest.tex` for the filter-row table cells and inline placeholders).

- **Step 5b — IN PROGRESS.** Direct access to `CHMM-Model` in the same parent folder, so we can now execute the code deliverables listed in `revision-code-todo.md` and swap numbers in as they land. Status per item:
  - **M3 (filter-based one-step-ahead VaR) — DONE (2026-04-24).** New script `CHMM-Model/run_track_c3_filter_var.jl` implements a log-space rescaled forward filter plus a bisection-based mixture-quantile, runs Kupiec / Christoffersen on the resulting breach sequences for CHMM-N / -t / -L at $\alpha \in \{0.01, 0.05\}$, and writes `results/track_c3/VaR_filter_LR_tests.txt` plus `CHMM-paper/results/robustness/filter_var_backtest.csv`. **Honest negative finding:** the filter-based one-step-ahead regime-conditional VaR fails Kupiec for all three emission families at both $\alpha$ levels on the 2024-2026 OoS window (breach rate $\approx 0.2\%$ vs.\ $1\%$ target; $1.2$--$1.4\%$ vs.\ $5\%$ target), i.e.\ systematically over-conservative. Mechanism: the forward filter places non-trivial posterior on multiple states, and the mixture $\alpha$-quantile of heavy-tailed per-state emissions is pulled deep into the left tail even when the bulk regime weight dominates. The smoother-based Viterbi diagnostic continues to pass Kupiec + Christoffersen for CHMM-t, validating the IS regime partition on the OoS stress window; the filter result reveals that an IS-fit flat CHMM does not yet translate that regime information into a Kupiec-calibrated operational VaR rule. Paper updates: Table~\ref{tab:conditional_var} filter rows now carry real numbers (no more `\revtodo{pending}`); abstract, introduction, results §CHMM-N paragraph, `var_backtest.tex` §sec:conditional_var opening and three-way split, and `discussion.tex` three-way operational split all rewritten to state this honest negative finding and name the candidate remedies (adaptive re-fitting on a rolling window, state-conditional variance overlay, tail-emission shrinkage per M10). Only `\revtodo` reference left in the paper is the macro definition itself at `paper.tex` L91.
  - **M10 ($\nu_k$ shrinkage, penalised ECM) — DONE (2026-04-24).** `CHMM-Model/run_nu_shrinkage_sweep.jl` implements a rate-sweep over $\lambda \in \{0, 5, 20, 50, 100, 200\}$ for the penalised objective $Q_k^{\text{pen}}(\nu) = Q_k(\nu) - \lambda / \nu$ (exponential shrinkage prior on $1/\nu_k$), fitted by the same golden-section search. Implementation added as a new `ν_shrink_rate` kwarg to `baum_welch_student_t` / `build(MyStudentTHiddenMarkovModel, ...)` with default 0 (backward compatible). Two findings:
    - **Kurtosis overshoot is fixable within the Student-t family.** Rate $\lambda \approx 20$ brings simulated IS excess kurtosis from $14.30$ (unpenalised) to $8.43$, close to the observed $7.69$, with IS KS pass rate essentially unchanged ($94.7\%$ vs.\ $95.7\%$). Only the lower-bracket state $\nu_{\min}$ moves ($2.1 \to 4.89$); median and max $\nu_k$ stay at $50$. The bracket-sweep diagnostic "only $\nu = \infty$ cures the overshoot" in the discussion has been replaced with the shrinkage-sweep finding.
    - **Shrinkage does NOT rescue filter-based VaR Kupiec.** Across the entire rate sweep, filter VaR at $\alpha = 0.01$ remains at breach rate $0.17\%$ with $\text{LR}_{\text{uc}} = 5.99$. This is a substantive cross-finding with M3: the over-conservatism mechanism is the mixture-over-states induced by the filter posterior, not the heavy tails of any one emission, so the remedy for the filter-VaR failure lies elsewhere (rolling refit, state-conditional variance overlay, or concentration-constrained posterior).
    - Paper integration: `sections/discussion.tex` §"Diagnosing and Shrinking Away the CHMM-t IS Kurtosis Overshoot" fully rewritten with the rate-sweep numbers; `sections/var_backtest.tex` §sec:conditional_var filter paragraph adds one sentence stating that shrinkage doesn't rescue Kupiec and names the candidate remedies; `sections/algorithms_appendix.tex` CHMM-t ECM paragraph adds the penalised objective equation (`eq:nu_pen`). CSV at `CHMM-paper/results/robustness/nu_shrinkage_sweep.csv`.
  - **M7 (expanded GARCH-family baseline panel) — PARTIAL DONE (2026-04-24).** Five of six new baselines implemented. New `src/GARCHFamily.jl` module adds EGARCH(1,1) Gaussian (Nelson 1991), GJR-GARCH(1,1) Gaussian (Glosten-Jagannathan-Runkle 1993 / Zakoïan 1994), GARCH(1,1) with Student-t innovations, and HAR-RV (Corsi 2009) on daily squared returns. `src/MSGARCH.jl` extended with a 3-regime MS-GARCH (`fit_msgarch_k3`) using a softmax-normalised transition-matrix parameterisation and the same grid + Nelder-Mead pattern as the K=2 case. `run_garch_suite.jl` fits all five new baselines plus the existing GARCH(1,1) and MS-GARCH K=2 references, simulates $N_{\text{paths}} = 1{,}000$ IS and OoS paths per model, and scores the full distributional + VaR panel. Highlights:
    - **GARCH-t** with $\hat\nu = 6.89$ is the headline new row (IS KS $57.3\%$ vs.\ GARCH(1,1)'s $25.9\%$; OoS KS $80.8\%$; kurtosis $15.13$ close to CHMM-t's $14.57$; Kupiec LR_uc $\approx 0.01$ at both $\alpha$). Directly answers the referee's call for a tail-aware GARCH competitor to CHMM-t.
    - **GJR-GARCH** with $\hat\gamma = 0.225$ quantifies the leverage effect in SPY IS.
    - **MS-GARCH K=3** state $\sigma = [0.10, 1.77, 10.04]$ (calm / medium / stress); best ACF-MAE in the panel ($0.0284$); modest KS improvement over K=2.
    - **HAR-RV on daily squared returns** is documented as a long-memory reference but is not a competitive return simulator (simulated kurtosis $188.95$ driven by log-RV residual variance $\hat\sigma_\eta = 2.449$ propagating through the exponential link); reported as an honest negative finding on HAR-RV-as-return-generator.
    - New table `tab:garch_suite` in `sections/results.tex` plus a dedicated subsection in `sections/baselines_appendix.tex` (§sec:m7_baselines). Four new citations added to `references.bib` (Nelson 1991, Glosten-Jagannathan-Runkle 1993, Zakoïan 1994, Corsi 2009), all rendered in `paper.bbl`. **FIGARCH** is deferred to a follow-up pass — requires a truncated-lag FI polynomial implementation that did not fit in this session.
  - **M4 (broaden empirical base) — PARTIAL DONE (2026-04-24); multi-index follow-up pending.** `run_rolling_and_weekly.jl` implements two of the three M4 sub-asks on SPY (no new data fetch needed; slices the existing 2014-2026 daily data by date).
    - **Rolling-origin OoS (five windows, 8-year IS + 1-year OoS across 2022-2026).** Produces one of the revision's most informative negative findings: **the 2022 OoS window (IS 2014-2021) fails catastrophically** (OoS KS 0.2-0.4% across all three emission families; Kupiec LR_uc at 5% is 31-41), because the rate-hike regime is a structural break outside the IS support. The 2023 window fails Kupiec at 5% (LR_uc=14). The 2024, 2025, and 2026-YTD windows pass Kupiec cleanly and sit at 87-97% OoS KS, consistent with the paper's single-window result. Aggregate picture: CHMM-t IS KS 96.4% ± 1.6%; OoS KS 91.1% ± 3.5% excluding the 2022 window (72.9% ± 38.6% including it). This directly answers the referee's M4 single-window concern with an honest positive-plus-negative reading.
    - **Weekly-frequency on SPY IS (5-day non-overlapping sum, T_weekly = 503, K ∈ {6, 12}).** All three emission families attain 98-99.6% IS KS at both K values; CHMM-t at K=6 produces simulated excess kurtosis 3.82 against observed 4.19 (closest match). The unified CHMM scaffold transfers to a coarser sampling frequency without re-architecture.
    - **Multi-index sub-ask deferred.** Alpaca credentials confirmed at `alpaca-markets-sdk/conf/apidata.toml`, but the fetch script for URTH (MSCI World) / EWG (DAX) / EWJ (Nikkei) / EWU (FTSE) daily bars from 2014-2026, along with the cross-index distributional panel, is a follow-up of ~100 lines of new code (fetch + JLD2 persist + integrate into the panel). Scoped in `revision-code-todo.md` M4.
    - Paper integration: new §sec:rolling\_and\_weekly in `sections/results.tex` with two new tables `tab:m4_rolling` and `tab:m4_weekly`; `sections/discussion.tex` limitations paragraph updated to cite the 2022 structural-break finding as direct evidence for the stationarity limitation. CSVs at `CHMM-paper/results/robustness/rolling_origin_oos.csv` and `weekly_frequency.csv`.
  - **M8 (pre-OoS K-selection) — DONE (2026-04-24).** `CHMM-Model/run_k_selection_validation.jl` implements clean K-selection on a validation slice carved from the original IS without touching the 2024-2026 OoS window. Estimation slice: 2014-01-03 through 2021-12-31 ($n = 2{,}013$). Validation slice: 2022-01-03 through 2024-01-03 ($n = 502$). Forward-algorithm held-out log-likelihood on the validation slice is the primary criterion. **Substantive finding:** the referee's concern is confirmed — K=18 was OoS-contaminated. Six independent non-OoS criteria all select K < 18:
    - Held-out log-lik: $K^\star = 3$ (val log-lik/obs $-2.5345$ vs. $-2.5694$ at $K = 18$).
    - BIC, HQC, CAIC: $K^\star = 3$.
    - AIC: $K^\star = 6$.
    - Held-out KS pass rate: $K^\star = 9$.
    - At no criterion that avoids the OoS window is $K = 18$ optimal.
    - Robustness check: CHMM-t / CHMM-L at $K \in \{12, 15, 18, 21\}$ show the same monotone-decreasing val log-lik pattern, so the $K^\star < 18$ ordering is not an emission-family artefact.
    - Paper integration: new paragraph "Pre-OoS validation K-selection (revision response to M8)" inside `sections/results.tex` §sec:model\_selection, with Table~\ref{tab:m8_k_selection} showing all seven K values and six criteria. Honest framing: $K = 18$ is kept as the main-panel operating point because the paper's contribution is the multi-metric evaluation (distributional + tail + VaR) rather than a pure likelihood-optimal fit; a reader optimising for held-out log-lik alone would pick $K = 3$, and the M4 rolling-origin result shows the $2022$-$2023$ validation slice is partly a structural-break period on which any model with IS-specific structure generalises worse. CSV at `CHMM-paper/results/robustness/k_selection_validation.csv`.
  - **M5 (Christoffersen LR_ind bootstrap null at $n = 572$) — DONE (2026-04-24).** `CHMM-Model/run_lr_ind_bootstrap_null.jl` simulates $B = 10{,}000$ i.i.d. Bernoulli$(\alpha)$ breach sequences of length $572$ at each $\alpha \in \{0.01, 0.05\}$ and computes Christoffersen LR_ind on each, producing the empirical null distribution. Findings:
    - At $\alpha = 0.01$ the asymptotic $\chi^2_1 = 3.84$ cutoff has **empirical size $2.07\%$** (not the nominal $5\%$); the test is *conservative* under-rejects at $n = 572$. Bootstrap 95% quantile: $1.65$ (well below asymptotic $3.84$).
    - At $\alpha = 0.05$ the empirical size is $4.20\%$ and the bootstrap 95% quantile is $3.80$ (close to asymptotic values).
    - Smoother CHMM-t $\text{LR}_{\text{ind}} = 0.09$ at $\alpha = 0.01$ has bootstrap $p = 0.52$ (firmly consistent with independence); at $\alpha = 0.05$ $\text{LR}_{\text{ind}} = 0.19$, $p = 0.67$. Both survive a sharper test than the asymptotic one.
    - **Filter CHMM-t $\text{LR}_{\text{ind}} = 8.74$ at $\alpha = 0.05$ has bootstrap $p = 0.002$** — a decisive rejection that confirms the M3 filter-VaR clustering failure is not a small-sample artefact. Filter CHMM-L $p < 0.001$ likewise.
    - At $\alpha = 0.01$ the filter $\text{LR}_{\text{ind}} = 0.00$ values are driven by a single observed breach; any zero- or one-breach sequence yields $\text{LR}_{\text{ind}} = 0$ by the standard estimator's convention. These cells are reframed in the paper as "test uninformative (degeneracy)" rather than "Christoffersen clearance."
    - Paper integration: new paragraph "Bootstrap null for $\text{LR}_{\text{ind}}$ at $n = 572$ (revision response to M5)" inside `sections/var_backtest.tex` §sec:conditional\_var, with bootstrap quantiles, p-values, and the degeneracy caveat for the $\alpha = 0.01$ filter rows. CSV at `CHMM-paper/results/robustness/lr_ind_bootstrap_null.csv`.
  - **M9 (skew-t / skew-Laplace emission ablation) — DONE (2026-04-24).** New `CHMM-Model/src/SkewEmissions.jl` adds Fernandez-Steel skew-t and skew-Laplace log-densities, samplers, and a weighted-MLE fitter for the skew parameter $\gamma$. `run_skew_emissions_ablation.jl` runs two ablations: (a) K=1 single-emission symmetric t vs skew-t; (b) K=18 plug-in skew-CHMM-t and skew-CHMM-L (symmetric CHMM fit is reused for transitions and per-state $(\mu_k, \sigma_k, \nu_k)$ or $(\mu_k, b_k)$, then per-state $\gamma_k$ fit by weighted MLE on the EM posteriors). **Substantive answer to the referee: the within-state skew ablation produces only minor improvements because the symmetric-emission, multi-state mixing already absorbs most of SPY's asymmetry.**
    - Symmetric CHMM-t at K=18 already produces simulated IS skewness $-0.667$ vs.\ observed $-0.752$.
    - Plug-in skew-CHMM-t brings simulated skewness to $-0.728$ (closer match) at a small IS-KS cost ($96.4\% \to 95.0\%$).
    - Per-state $\gamma_k$ values cluster tightly around 1 (range 0.96-1.02, median 1.00, zero of 18 states with $|\gamma_k - 1| > 0.05$). Same pattern for CHMM-L.
    - Paper integration: `sections/discussion.tex` Limitations bullet on emission symmetry rewritten to report the ablation result. The "natural extension" placeholder is replaced by the quantified answer that within-state skew is a genuinely small residual gap. CSV at `CHMM-paper/results/robustness/skew_emissions_ablation.csv`.
  - **M2 (block-bootstrap KS recalibration + mean p-value distribution) — DONE (2026-04-24).** `CHMM-Model/run_ks_block_bootstrap.jl` implements stationary block bootstrap (Politis-Romano 1994) of the SPY IS series at mean block lengths $L \in \{5, 10, 20\}$, $B = 1{,}000$ replications, and reports for each generator (CHMM-N / -t / -L, GARCH(1,1), iid bootstrap) the asymptotic pass rate, mean p-value, p-value quantiles, and block-bootstrap pass rates. Findings:
    - Block-bootstrap 95% KS critical values: $0.0306$ ($L = 5$), $0.0338$ ($L = 10$), $0.0350$ ($L = 20$); asymptotic value $0.0383$. Block-aware test is *stricter* than the asymptotic one.
    - **GARCH(1,1) drops from 25.4% asymp $\to$ 5.6%/11.2%/16.2% block-aware** — directly confirms the referee's concern that the asymptotic test is lenient on volatility-clustering generators with the wrong marginal.
    - **CHMM family stays stable: 94.8% asymp $\to$ 88-92% block-aware**, indicating the asymptotic-test passes are not a clustering artefact.
    - Mean p-value scale: bootstrap $0.812$, CHMM-t $0.571$, CHMM-N $0.546$, CHMM-L $0.498$, GARCH $0.042$. CHMM family clusters at mean p-value $\sim 0.5$, the natural value for indistinguishable paths.
    - Paper integration: new §sec:ks\_block\_bootstrap in `sections/baselines_appendix.tex` immediately after the existing KS power calibration subsection, with Table~\ref{tab:m2_ks_bootstrap} and interpretive paragraphs. CSV at `CHMM-paper/results/robustness/ks_block_bootstrap.csv`.
  - **M6 (sub-ask b: MC CI on the Kupiec gain) — DONE (2026-04-24); sub-ask a (Yu 2010 explicit-duration MLE) deferred.** `CHMM-Model/run_kupiec_mc_ci.jl` parametric-bootstraps the LR_uc statistic at $n = 572$, $\alpha = 0.05$ under each (model, observed breach rate $\hat p$) pair, giving the MC noise band the observed $\text{LR}_{\text{uc}}$ inhabits. Findings:
    - The MC band on $\text{LR}_{\text{uc}}$ at $n = 572$ is wide ($\sigma \approx 2$-$4$), confirming the referee's concern that the point-estimate gain $3.83 \to 0.82$ is smaller than the noise on either statistic taken alone.
    - Pairwise comparison (independent draws under flat vs. SM rate): the SM rate produces a strictly smaller $\text{LR}_{\text{uc}}$ in **76% of MC replications for CHMM-N**, **65% for CHMM-t**, and 47% for CHMM-L (no effect because the L breach rate did not move). The unconditional-VaR calibration gain on the N and t variants is therefore genuine in the majority of MC replications but smaller in magnitude than the point-estimate alone suggests.
    - Paper integration: new "MC confidence band on the Kupiec gain (revision response to M6)" paragraph in `sections/var_backtest.tex` immediately after the existing tab:sm_var, with bootstrap means / quantiles / pairwise overlap and an explicit acknowledgement that the Yu 2010 explicit-duration MLE is a deferred follow-up. CSV at `CHMM-paper/results/robustness/kupiec_mc_ci.csv`.
    - Sub-ask (a) — Yu (2010) explicit-duration forward-backward MLE for SM-CHMM — remains a follow-up. Documented in `revision-code-todo.md` as ~300-400 lines of new HSMM EM code; the plug-in numbers in the paper are now defended by the MC CI argument above pending that follow-up.
  - **Tier-3 minors 4, 6, 10 — DONE (2026-04-24).** All three implemented as standalone scripts and integrated into a single new §sec:tier3\_robustness subsection in `sections/baselines_appendix.tex`.
    - **Minor 4 (fixed observed-sample MMD bandwidth):** `run_mmd_fixed_bandwidth.jl` recomputes MMD across the M7 generator panel under a fixed $\gamma_{\text{obs}}$ from observed 20-day windows. Legacy and fixed MMDs differ only in the 3rd-4th decimal across all ten generators; cross-generator ranking is preserved. The bandwidth-heuristic concern is real in principle but small in practice for this panel. CSV at `mmd_fixed_bandwidth.csv`.
    - **Minor 6 ($K_{\text{disc}} = 13$ centroid ablation):** `run_kdisc13_centroid_ablation.jl` fits a discrete HMM at $K_{\text{disc}} = 13$ with centroid emissions (no jumps, no Student-t). IS KS = $0.0\%$ vs Bin-T NJ's $95.4\%$ at the same bin count. Cleanly isolates that the Bin-T NJ row's calibration is driven by the within-bin Student-t emissions, not the bin count. CSV at `kdisc13_centroid_ablation.csv`.
    - **Minor 10 (multi-seed MC):** `run_multiseed_headline.jl` runs 10 alternative global seeds of the CHMM-N/-t/-L pipeline. Seed-to-seed std is small (IS KS ±1.2-1.8%; OoS KS ±1.8-3.6%; CHMM-N kurt ±0.06; CHMM-t kurt ±1.95 reflecting the known $\nu_k$ ECM instability). Headline single-seed numbers sit comfortably within multi-seed bands. New table `tab:m10_multiseed` in the appendix. CSV at `multiseed_headline.csv`.
  - **Remaining items (M7 FIGARCH follow-up; M4 multi-index follow-up; M6 Yu MLE follow-up):** scoped in `revision-code-todo.md` as separate code-repo passes. All twenty referee items (M1-M10 plus minors 1-10) now have substantive paper-side responses.
  - Resubmission gate: zero `[TODO (revision): ...]` cells in the final PDF (currently: zero cells left in the body; the macro definition in the preamble is retained so future re-runs can reintroduce placeholders if needed).
